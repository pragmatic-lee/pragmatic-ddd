# 项目概述
* 项目名称：pragmatic-ddd
* 核心功能：项目是一个领域驱动设计的框架，包含实体聚合根规则、领域事件、仓储等基础领域驱动设计框架能力，用了 RocketMQ、Kafka 以及 MyBatis 的一些能力集成
* 核心技术栈：是基于Java 17版本开发

# 目录结构

* pragmatic-ddd-core 该框架的核心基础结构包
* pragmatic-ddd-mybatis 是提供的一些Mybatis的一些能力
* pragmatic-ddd-rokect-mq 是领域事件基础设施rocketMQ实现包
* pragmatic-ddd-kafka 是领域事件基础设施kafkaMQ的实现包
* pragmatic-ddd-spring-boot 和SpringBoot集成的包
* pragmatic-ddd/docs 里面放的是一些提案文档以及设计文档,我的设计文档统一放到这个目录下面，它可以根据代码的包去分类。
* pragmatic-ddd/documentation 这个目录是放使用文档的地方

# 核心工作原则
* 这是一个领驱动的框架库，所以在设计时要考虑框架的通用性，以及和其他项目引用该框架后的易于集成
* 基于 Java 17 版本开发，在设计和实现代码时，尽量使用 Java 17 版本的特性。
* 一些控制针判断不要用三元表达式，可以采用 Optional 的方式
* 集合类型的处理：如果能使用流，就使用流；如果不能，退回到 for each。
* 代码注释，注释的话类级别的注释和公有方法的注释，私有方法不用加注释，注释的要求仅说明这个类或这个方法是干什么的，不要在注释里面增加很多的示例代码，代码里注释里面可以有作者统一使用 wizard-lee,注释里面不用包含其他的版本信息之类的
* 在有任何变动之前需要输出文档，如果没有文档不能改代码，我仅仅是让你去分析一下原因，没有输出文档，那你仅输出分析一下的原因，不要改代码。
* 定义接口时，接口一定要以I开头
* 无论多简单的 if 都需要带花括号，不要出现那种不带花括号的 if 逻辑
* 一些实体类、DTO 类，或者是数据容器类，可以用 Lombok 去做简化
* 如果Lambda 可以被方法引用替代，优先使用方法引用

# 安全与兜底策略
* 禁止破坏性操作：未经明确确认，严禁执行 rm -rf、git push --force 或清空数据库等高危命令。
* 信息不足时：如果需求模糊或缺少必要上下文，请先列出需要澄清的问题，不要瞎编代码。
* 敏感信息：严禁在代码中硬编码 API Key、密码或内部 IP，必须使用环境变量。